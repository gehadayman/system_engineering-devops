#!/usr/bin/env bash
# This script lists running processes with formatted output

ps -eo user,lstart,comm --sort=start_time | awk 'NR==1 {print "USER STARTED COMMAND"; next} {printf "%s %s %s %s %s %s %s\n", $1, $2, $3, $4, $5, $6, $7}'
